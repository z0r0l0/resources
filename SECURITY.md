# 🔒 安全约定

## 这个仓库是什么

个人资源库：系统配置、脚本、知识文档、项目模板。**公开可读**，因此适用以下约定。

## 绝不入库的内容

`.github/workflows/repo-guard.yml` 会在每次推送时自动拦截下列内容，本地可用
`bash scripts/system/repo-guard.sh` 提前自查（同一份实现）：

| 类别 | 拦截规则 |
|------|----------|
| 密钥特征 | `ghp_*`、`github_pat_*`、`sk-*`、`AKIA*`、`AIza*`、`xox[baprs]-*` |
| 私钥内容 | `-----BEGIN ... PRIVATE KEY-----` |
| 敏感文件 | `.env`、`.pem`、`.key`、`.p12`、`.pfx`、`id_rsa`、`id_ed25519`、`.git-credentials` |
| 超大文件 | 单个文件 > 5MB（应改用 Git LFS） |

## 主机专属信息不在此仓库

本仓库公开，因此**不承载**以下内容：

- 权限审计基线的具体结果（账户名、挂载盘符、风险登记表）
- 任何含主机标识符的配置
- 凭据、Token、私钥

权限审计的**方法论**在 `system/security/README.md`（通用、可复用）；
具体结果在私有仓库 `hermes-private` 中保存。

判断标准：**「如果陌生人读到这行，能否据此定位或攻击我的机器？」**
能，就属于私有仓库。

## 文档脱敏规范

写入本仓库的文档在提交前检查：

- 账户名、主机名、内网地址 → 用 `<user>`、`<host>` 占位
- AppID、OpenID、设备标识 → 隐去，只说明存在
- 端口、路径、盘符 → 保留（复用价值高于风险）
- 命令示例中的密钥 → 用 `<your-token>` 占位

## 报告问题

发现本仓库泄露了敏感信息，或守卫存在绕过方式：

- 邮箱：`3036186400@qq.com`
- 或直接开 Issue（**不要在 Issue 里粘贴任何真实凭据**）

收到后我会清理仓库历史并轮换受影响凭据。

## 许可

本项目以 [MIT](LICENSE) 许可发布。你自由使用，但不对生产环境适用性作任何担保。
