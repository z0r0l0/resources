# 🤝 贡献指南

## 这是什么

个人资源库，由 Hermes Agent 维护。公开是为了复用价值——配置、脚本、踩坑记录
对同样在 WSL 上折腾的人有用。

- **欢迎**：Issue 报错、指出文档里过时或误导的内容、提 PR 修正
- **不承诺**：响应时间，以及合并「方向不同」的改动（这是个人环境，不是通用框架）

## 提交前必做

```bash
# 1. 跑守卫（与 CI 同一份实现，不通过 CI 必红）
bash scripts/system/repo-guard.sh

# 2. 确认没有把主机专属信息写进文档
#    标准见 SECURITY.md —— 「陌生人读到能否据此攻击我的机器」
```

## 文件规范

### 脚本（`scripts/`、`system/`、`tools/`）

沿用现有格式，保持风格一致：

```bash
#!/bin/bash
# ============================================================================
# <Script Title>
# Managed by Hermes Agent
# ============================================================================

set -uo pipefail                    # 脚本一律开严格模式

GREEN='\033[0;32m'                  # 颜色变量按现有命名
NC='\033[0m'
```

要求：
- 中文注释，关键步骤逐行说明**为什么**这么做，而不只是做什么
- 版本钉死（示例：`NODE_VERSION=22.11.0`，不用 `latest`）
- 幂等：重复执行不产生副作用
- 有明确的退出码语义

### 文档（`knowledge/`、各目录 `README.md`）

- 中文，emoji 二级标题（与现有文档一致）
- 可复制执行的命令块，不用截图代替命令
- 排错指南写「现象 → 原因 → 解决」，不写理论长篇

## 提交信息规范

采用 [Conventional Commits](https://www.conventionalcommits.org/zh-hans/)：

```
<类型>(<范围>): <简述>

<正文：为什么这么改，而不是改了什么>
```

类型：`feat` `fix` `docs` `chore` `refactor` `security`

示例：

```
fix(security): 修正权限基线 v1.1 — /mnt/c 盘根实为只读

v1.0 只测了用户子目录就外推到整个盘。复核实测盘根 touch 被拒。
```

## 目录归类

新增文件前先确认归属：

| 目录 | 放什么 | 不放什么 |
|------|--------|----------|
| `system/` | 主机配置、Shell 环境、代理、安全 | 可执行工具 |
| `scripts/` | 可执行的运维/开发脚本 | 配置样本 |
| `knowledge/` | 技术文档、排错指南 | 一次性笔记 |
| `templates/` | 项目脚手架 | 具体项目代码 |
| `tools/` | 一键安装、环境初始化 | 日常脚本 |

主机专属内容 → 私有仓库 `hermes-private`。

## 文档变更要同步

改了目录结构或新增目录，记得同步更新：

- 顶层 `README.md` 的目录树
- 对应目录的 `README.md`
- `CHANGELOG.md`
